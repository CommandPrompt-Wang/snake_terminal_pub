# Auto detect text files and force LF line endings
* text=auto eol=lf

# Windows batch files require CRLF
*.bat text eol=crlf

# Polyglot routers: mixed CRLF (batch) + LF (shell); keep as-is
build.bat -text
pack.bat -text

# windows enigma packing
pack-windows.evb text eol=crlf